<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/teacher-profile.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/footer.css">
    <title>Teacher Profile</title>
</head>
<body>
    <%- include('in-app-header') %>

    <!-- Navbar -->
    <nav class="navbar">
        <ul class="nav-list">
            <li><a href="/teacher" class="nav-link">Home</a></li>
            <li><a href="/teacher/classes" class="nav-link">Classes</a></li>
            <li><a href="/teacher/profile" class="nav-link">Profile</a></li>
        </ul>
    </nav>

    <div class="main">
        <h1>My Profile</h1>
        
        <!-- Display Current Information -->
        <div class="profile-info" id="profile-info">
            <p><strong>ID:</strong> <%=teacher.teacher_id %></p>
            <p><strong>Name:</strong> <%= teacher.teacher_name %></p>
            <p><strong>Email:</strong> <%= teacher.teacher_email %></p>
            <p><strong>Faculty:</strong> <%= teacher.teacher_faculty %></p>
        </div>

        <!-- Button to Reveal Password Form -->
        <button onclick="togglePasswordForm()" class="edit-btn">Change Password</button>

        <!-- Password Update Form, Hidden Initially -->
        <form action="/teacher/profile/update-password" method="POST" id="password-form" class="password-form" style="display: none;">
            <h2>Change Password</h2>

            <label for="current-password">Current Password:</label>
            <input type="password" id="current-password" name="currentPassword" required>

            <label for="new-password">New Password:</label>
            <input type="password" id="new-password" name="newPassword" required>

            <label for="confirm-password">Confirm New Password:</label>
            <input type="password" id="confirm-password" name="confirmPassword" required>
            
            <button type="submit" class="save-btn">Update Password</button>
            <button type="button" onclick="togglePasswordForm()" class="cancel-btn">Cancel</button>
        </form>
    </div>

    <script>
        function togglePasswordForm() {
            const passwordForm = document.getElementById('password-form');
            // Toggle visibility of password form
            passwordForm.style.display = passwordForm.style.display === 'none' ? 'block' : 'none';
        }
    </script>

    <%- include('footer') %>
</body>
</html>
